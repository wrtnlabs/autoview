import { Components } from "@mui/material";

const recipe: Components["MuiAccordion"] = {
  styleOverrides: {
    root: {
      width: 400,
      borderRadius: 5,
      boxShadow: "none",
    },
  },
};

export default recipe;
