import { Components } from "@mui/material";

const recipe: Components["MuiList"] = {
  styleOverrides: {
    root: {
      minWidth: 360,
    },
    subheader: {
      width: "100%",
    },
  },
};

export default recipe;
