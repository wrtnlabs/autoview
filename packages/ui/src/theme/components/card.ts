import { Components } from "@mui/material";

const recipe: Components["MuiCard"] = {
  styleOverrides: {
    root: {
      borderRadius: 1,
      backgroundColor: "#000",
      color: "#fff",
    },
  },
};

export default recipe;
