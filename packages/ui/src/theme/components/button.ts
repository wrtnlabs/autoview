import { Components } from "@mui/material";

const recipe: Components["MuiButton"] = {
  styleOverrides: {
    root: {
      borderRadius: 4,
      boxShadow: "none",
    },
  },
};

export default recipe;
